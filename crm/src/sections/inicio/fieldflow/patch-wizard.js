const fs = require("fs"); const f = "Z:/Diseño V2/GARZA/06-GarzaPage/crm/src/sections/inicio/fieldflow/FieldFlowWizard.jsx"; let c = fs.readFileSync(f, "utf8"); c = c.replace("function WizardContent({ onClose, onSuccess }) {", "function WizardContent({ onClose, onSuccess }) {\\n  const [isExiting, setIsExiting] = useState(false);"); const fn = "  const handleAnimatedClose = () => {\\n    setIsExiting(true);\\n    setTimeout(() => {\\n      if (typeof onClose === \\x27function\\x27) onClose();\\n    }, 220);\\n  };"; c = c.replace("const { step, direction, paginate, wizardState } = useFieldFlow();", "const { step, direction, paginate, wizardState } = useFieldFlow();\\n" + fn); c = c.replace("return (\\r\\n    <div className=\\\x22fieldflow-wizard-overlay\\\x22>", "return (\\r\\n    <div className={ieldflow-wizard-overlay \}>\\n      <div className={ieldflow-wizard-card \}>"); c = c.replace("</main>\\r\\n    </div>\\n  );", "</main>\\n      </div>\\n    </div>\\n  );"); c = c.split("onClick={onClose}").join("onClick={handleAnimatedClose}"); const ot = "setTimeout(() => {\\n        if (typeof onClose === \\x27function\\x27) onClose();\\n      }, 2500);"; const nt = "setTimeout(() => {\\n        handleAnimatedClose();\\n      }, 2500);"; c = c.split(ot).join(nt); fs.writeFileSync(f, c, "utf8"); console.log("Done!");